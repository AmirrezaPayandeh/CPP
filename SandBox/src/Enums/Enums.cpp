// Enums

/*
 * enums are just group of integers that are given name to them. they can make code readable.
 */
enum MyTestEnum
{
	// if you don't specify their value, it will start from 0, and increase for each one of them.
	// meaning that B is now 1 and C is 2.
	// if you start A from 5, B would be 6 and C would be 7.
	A = 0, B, C
};